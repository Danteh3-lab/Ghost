[build]
  command = "npm run build && cp -r payloads dist/"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  framework = "vite"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
